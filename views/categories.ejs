<% if(!locals.categories || locals.categories.length === 0) { %>
<h1>No categories found</h1>
<% } else { %> <% locals.categories.forEach(category => { %>
<a href="/categories/<%= category.id %>"> <%= category.name %> </a>
<% }); } %>
