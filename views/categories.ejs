<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Inventory Management</title>
    <link rel="stylesheet" href="/styles/main.css" />
  </head>
  <body>
    <div id="container">
      <% if(!locals.categories || locals.categories.length === 0) { %>
      <h1 class="title">No categories found</h1>
      <% } else { %>
      <h1 class="title">Categories:</h1>
      <div id="grid">
        <% locals.categories.forEach(category => { %>
        <a class="grid-item" href="/categories/<%= category.id %>"> <%= category.name %> </a>
        <a class="grid-item" href="/categories/<%= category.id %>/update">Update</a>
        <% }); } %>
      </div>
      <div class="action-buttons-div">
        <a class="action-button" href="/categories/new">Create a category</a>
        <a class="action-button" href="/items/new">Create an item</a>
      </div>
    </div>
  </body>
</html>
